import { View } from 'react-native';
import AppStack from './AppStack';

function Navigation() {
  return (
    <View style={{ flex: 1 }}>
      <AppStack />
    </View>
  );
}

export default Navigation;
